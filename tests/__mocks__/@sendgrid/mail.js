module.exports = {
  setApiKey() {
    // just returning undefined...don't need to actually do anything with the api key in a test
  },
  send() {
    // just returning undefined...this mock ensures emails aren't actually sent during a test
  }
};
